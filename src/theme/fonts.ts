/**
 * Brand wordmark: round, friendly (Fredoka — loaded in app/_layout.tsx).
 * Use with `fontFamily` only; do not combine with heavy `fontWeight`.
 */
export const fontBrand = 'Fredoka_700Bold';

/** Softer / bubblier titles (splash, sign-in). */
export const fontBrandSoft = 'Fredoka_600SemiBold';
